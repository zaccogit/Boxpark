import Colors from './Colors';
import AxiosEndPointsConfig from './AxiosEndPointsConfig';
import { Dimensions } from 'react-native';

export const {height, width} = Dimensions.get("window");

export {
    Colors, 
    AxiosEndPointsConfig
};
